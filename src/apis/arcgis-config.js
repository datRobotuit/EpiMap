// Cấu hình ArcGIS API
export const configureArcGIS = () => {
  // Bạn có thể thêm API key ở đây nếu có
  // window.esriConfig = {
  //   apiKey: "YOUR_API_KEY_HERE"
  // };
  
  // Hoặc bạn có thể sử dụng bản đồ công khai mà không cần API key
  // Nhưng điều này có thể bị giới hạn về số lượng truy cập
  
  console.log('ArcGIS configuration loaded');
};
